/*
Auto-generated by: https://github.com/pmndrs/gltfjsx
Command: npx gltfjsx@6.5.3 .\Pig.glb --transform -o C:\Users\emanu\next-portfolio\public\pig.jsx --shadows -f png 
Files: .\Pig.glb [305.95KB] > C:\Users\emanu\next-portfolio\public\Pig-transformed.glb [70.16KB] (77%)
*/

import React from "react";
import { useGraph } from "@react-three/fiber";
import { useGLTF, useAnimations } from "@react-three/drei";
import { SkeletonUtils } from "three-stdlib";
import { useAnimalAnimation } from "@/components/3D/entities/useAnimalAnimations";

export function Pig(props) {
  const group = React.useRef();
  const { scene, animations } = useGLTF("/models/animals/Pig-transformed.glb");
  const clone = React.useMemo(() => SkeletonUtils.clone(scene), [scene]);
  const { nodes, materials } = useGraph(clone);
  const { actions } = useAnimations(animations, group);

  useAnimalAnimation(actions);

  return (
    <group ref={group} {...props} dispose={null}>
      <group name="Scene">
        <group name="AnimalArmature">
          <primitive object={nodes.All} />
        </group>
        <skinnedMesh
          name="Pig"
          geometry={nodes.Pig.geometry}
          material={materials.Atlas}
          skeleton={nodes.Pig.skeleton}
        />
      </group>
    </group>
  );
}

useGLTF.preload("/models/animals/Pig-transformed.glb");
